import BaseSetSelector from "../../components/BaseSetSelector";

export const metadata = {
  title: "PACKWORKS Base Set Builder",
  description: "A stable 2D workspace for selecting a new PACKWORKS base set.",
};

export default function SubsetPage() {
  return <BaseSetSelector />;
}
